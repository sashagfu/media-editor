#import "../../responses/fragments/responseGeneric.graphql"

mutation attachProjectToPlaylist($projectId: ID!, $playlists: [ID]) {
  attachProjectToPlaylist(projectId: $projectId, playlists: $playlists) {
    ...responseGeneric
  }
}
