mutation createUser($email: String!, $username: String!, $displayName: String!, $password: String!) {
    createUser(email: $email, username: $username, displayName: $displayName, password: $password) {
        accessToken
        refreshToken
        expiresIn
        tokenType
    }
}