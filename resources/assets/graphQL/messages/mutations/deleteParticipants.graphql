#import "../fragments/threadGeneric.graphql"

mutation deleteParticipants($threadId: ID!, $ids: [ID]!, $userId: ID) {
    deleteParticipants (threadId: $threadId, ids: $ids) {
        ...threadGeneric
    }
}
