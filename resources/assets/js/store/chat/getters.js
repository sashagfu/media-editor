export default {
  activeThread: state => state.activeThread,
  chatPage: state => state.chatPage,
  unreadMessagesCount: state => state.unreadMessagesCount,
  widgetVisibility: state => state.widgetVisibility,
};
