export default {
  activeUser: state => state.activeUser,
  unreadNotificationsCount: state => state.unreadNotificationsCount,
  feedItems: state => state.feedItems,
  homePageActiveBox: state => state.homePageActiveBox,
  newProject: state => state.newProject,
  newProjectId: state => state.newProject.id,
};
