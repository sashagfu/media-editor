import * as settings from 'Editor/config/settings';

export default {
  pxPerSec: settings.PX_PER_SEC,
  itemThumb: settings.ITEM_THUMB,
  timelineDuration: settings.TIMELINE_DURATIONS * 60000,
  maxDuration: settings.MAX_DURATIONS,
};
